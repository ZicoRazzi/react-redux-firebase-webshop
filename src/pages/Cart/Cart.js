import React from 'react';
import Checkout from './../../components/checkout/Checkout';
import './styles.scss';

const Cart = ({}) => {
  return (
    <div className="cart">
      <Checkout />
    </div>
  );
};

export default Cart;
